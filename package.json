{
  "name": "write-json-files",
  "version": "1.0.1",
  "description": "A module creating saving data from many sources to json files",
  "main": "dist/index.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/jeffreyyoung/write-json-files"
  },
  "scripts": {
    "build": "babel src -d dist",
    "prepublish": "npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-plugin-transform-object-rest-spread": "^6.26.0",
    "babel-plugin-transform-runtime": "^6.23.0",
    "babel-preset-env": "^1.6.1",
    "node-fetch": "^1.7.3"
  },
  "dependencies": {
    "babel-runtime": "^6.26.0"
  }
}
